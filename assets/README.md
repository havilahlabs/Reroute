# Assets

This directory intentionally avoids committed binary files.

Required Expo PNG assets are generated locally by `scripts/generate-assets.mjs` via npm lifecycle hooks before app commands run.
